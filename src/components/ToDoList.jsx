import ToDoTask from "./ToDoTask";

const ToDoList = (props) => {
  return <ToDoTask />;
};

export default ToDoList;

{
  /*   <div>
      {tasks.map((task) => {
        return <ToDoTask />;
      })}
    </div> */
}
